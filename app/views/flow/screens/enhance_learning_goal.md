# Enhance Learning Goal

The learner has the opportunity to add child `LearningGoal`s to a `LearningGoal` to generate a more fine-grained learning journey.

Furthermore, the learner may add `UnitOfMeaning`s to the `LearningGoal`. This interaction works via the fairly complex template include `edit_units_of_meaning`, which the view will have to handle correctly.